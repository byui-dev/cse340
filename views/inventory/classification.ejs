<% if (title) { %>
    <h1> <%= title %></h1>
    <% } %>

    <%- nav %>

    <%- grid %>

<% if (messages && messages.length)  { %>
    <ul class="messages">
        <% messages.forEach(msg) => { %>
            <li><%= msg %></li>
        <% }) %>
    </ul>
<% } %>    